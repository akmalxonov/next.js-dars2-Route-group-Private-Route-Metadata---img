import React from "react";

const LoginPage = () => {
  return (
    <div>
      LoginPage
      {/* Route Grouping  > () <    */}
      {/* Bu yerda (auth) — route group, ya'ni URL'ga ta'sir qilmaydigan papka.
      URL quyidagicha bo'ladi:
      /login
      /register
      Papka (auth) bo‘lsa ham, bu URL da ko‘rinmaydi. */}
    </div>
  );
};

export default LoginPage;
